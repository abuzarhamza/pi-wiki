# Installing Node.js

## macOS
The recommended way to install Node.js on macOS is using Homebrew:
```bash
brew install node
```

## Windows
The recommended way to install Node.js on Windows is using the official installer from [nodejs.org](https://nodejs.org/). Download the LTS version and run the installer.

Alternatively, you can use a version manager like `nvm-windows`.
